# docker-compose root .env (override what's in docker-compose.yml).
# Copy to .env and edit. NEVER commit the real .env.

APP_SECRET_KEY=replace-with-long-random-secret-min-32-chars
WHATSAPP_SERVICE_API_KEY=replace-with-long-random-api-key
